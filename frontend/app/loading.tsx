import { BrandLoader } from '@/components/ui/BrandLoader';

/** Next.js route-level loading UI — Dream / Discipline / Defend brand loader. */
export default function Loading() {
  return <BrandLoader fullScreen />;
}
